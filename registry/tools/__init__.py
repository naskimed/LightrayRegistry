"""t=0 converters (B7) — all idempotent, provenance-stamped imported:true.

Values are read from seed/*.json (which carry their provenance) or from real inputs on disk —
never typed inline (the no-remembered-literals rule applies to code too).
"""
